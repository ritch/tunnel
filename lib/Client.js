// #Client
// Connects to a public proxy server and internally proxies data back to 127.0.0.1
exports = module.exports = function(host) {
	
	// ##Connect
	// Connect to public proxy and pipe data to/from it.
	var connection;
	
};